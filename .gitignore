# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Private / per-machine settings (keeps your phone number out of the repo)
regime/local_settings.py

# Local runtime state & logs
data/notify_state.json
reports/
*.log

# Cached market data & signals (regenerated live; not needed in the repo)
data/cache/

# But DO track the daily signal history (the hosted dashboard accumulates it)
!data/signal_history.csv
